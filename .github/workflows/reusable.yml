name: Reusable Workflow

on:
  workflow_call:

jobs:
  test-call:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: List files
        run: ls -R
        