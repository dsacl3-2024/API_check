name: validator

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      input_var:
        required: true
        type: string

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Check out the reusable workflow repository (validator repository)
      - name: Checkout reusable repository
        uses: actions/checkout@v3
        with:
          # Specify the repository where the reusable workflow resides
          repository: dsacl3-2024/API_check
          ref: main # Replace 'main' with the branch name if necessary
          
      # Step 2: Debug: Verify that validate.py is available
      - name: List all files
        run: ls -R
              
        
#     # Step 2: Run the Python script
#     - name: Run Python script
#       run: python3 validate.py ${{ inputs.input_var }}