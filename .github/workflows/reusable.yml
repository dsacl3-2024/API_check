name: validator

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      input_var:
        required: true
        type: string

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
     # Step 1: Check out the reusable workflow's repository
      - name: Checkout reusable repository
        uses: actions/checkout@v3
        with:
          repository: ${{ github.repository }} # Ensure the reusable repo is checked out
          ref: main # Replace 'main' with the correct branch if needed
          
      # Step 2: Debug: Verify contents of the reusable repository
      - name: List all files
        run: ls -R
        
#     # Step 2: Run the Python script
#     - name: Run Python script
#       run: python3 validate.py ${{ inputs.input_var }}